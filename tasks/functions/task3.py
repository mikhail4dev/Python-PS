# **Описание**: Напишите функцию `rectangle_info`, которая принимает ширину и высоту
# прямоугольника и возвращает его площадь. Снабдите функцию докстрингом,
# описывающим параметры и возвращаемое значение. После объявления функции
# выведите её докстринг через `__doc__` и результат двух вызовов.
#
# **Входные данные**:
# - width (float) — ширина прямоугольника
# - height (float) — высота прямоугольника
#
# **Выходные данные**:
# - Сначала выводится докстринг функции (через print(rectangle_info.__doc__))
# - Затем результат вызова rectangle_info(4.0, 3.0) — число 12.0
# - Затем результат вызова rectangle_info(5.0, 2.5) — число 12.5
#
# **Ограничения**:
# - Функция должна именно возвращать площадь, а не печатать её
# - Докстринг обязателен и должен описывать оба параметра и возвращаемое значение
# - Используйте только стандартные конструкции Python без импортов

def rectangle_info(height: float, width: float) -> float:
    # Ваш код здесь
    """"
    Функция принимает 2 аргумента высоту и ширину прямоугольника, а возвращает его площадь
    """
    return height * width


# Ваш код здесь

print(rectangle_info.__doc__)
print(rectangle_info(4.0, 3.0))
print(rectangle_info(5.0, 2.5))